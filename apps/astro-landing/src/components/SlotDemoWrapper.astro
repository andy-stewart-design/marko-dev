---
import SlotDemo from "../tags/slot-demo/index.marko";
---

<SlotDemo title="Card.marko">
  <p>This is the <strong>default slot</strong> — passed as <code>input.content</code>.</p>
  <p slot="footer">This is a <em>named slot</em> — passed as <code>input.footer</code>.</p>
</SlotDemo>
